% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_examples.R
\name{export_examples}
\alias{export_examples}
\title{Export teaching datasets to files for data-import practice}
\usage{
export_examples(dir, which = "all", seed = 2026)
}
\arguments{
\item{dir}{Directory to write files into (created if needed).}

\item{which}{Character vector of dataset keys to export, or \code{"all"}.}

\item{seed}{Integer seed so the injected messiness is reproducible.}
}
\value{
(invisibly) a character vector of the file paths written.
}
\description{
Writes file copies of the canonical malaria teaching datasets — derived from the
master \code{case_linelist} and \code{environment_weekly} objects (plus the
analysis/genetics objects) — into a directory, in the formats and with the
deliberate "messiness" that the Week 3+ import and cleaning problem sets expect.
}
\details{
The point is that the file a student imports is provably the same data they
already met via \code{library(RCodingSupport)}: the package is the single source of
truth, and file import is just one way to reach it.
}
\examples{
\dontrun{
dir.create("data_raw")
RCodingSupport::export_examples("data_raw")
}
}
